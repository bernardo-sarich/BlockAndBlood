/// <summary>
/// Rarity tier for cards offered at level-up. Used by XPManager.GetRarityForCurrentLevel()
/// to weight the card selection pool.
/// </summary>
public enum CardRarity
{
    Common,
    Rare,
    Epic,
}
